# 進捗メモ(次回はここから)

## 今の状態

- データ: `data/honda`, `data/subaru` 配下のMarkdown(282文書 / 1250チャンク)
- Phase 1〜5(取込・ベクトル化・CLI検索・API・画面)は動作確認済み
- 起動コマンド:
  ```
  cd "C:\Users\DIT-G-071\Desktop\work\AI\2026\Project\work\my_app\SpecSemanticPoc"
  python -m uvicorn app.main:app --host 127.0.0.1 --port 8000
  ```
  → http://127.0.0.1:8000/
- データを変更したときだけ `python app/ingest.py` → `python app/embed.py` を再実行する

## データ方針(2026-09-17追記)

仕様書は当分PDFではなく、今あるMarkdown(`data/honda`, `data/subaru`)が中心になる見込み。
そのため「PDFのしおりが粗い場合の章階層復元」は当面優先度低。現状のMarkdownは見出し(#, ##)が
きちんと付いているため、その問題自体があまり発生しない。

## オリジナルにあってこれに無いもの(全リスト・1つずつ理解しながら進める)

ユーザーが「順番に理解しながら進めたい」とのことなので、精度への効果順に並べた全項目。
1つずつ「これは何をするものか」を説明→実装→動作確認、を1セットとして進める。

1. **前後文脈の表示**
   - 今: ヒットしたチャンクの抜粋だけをカードに表示
   - オリジナル: ヒット箇所の前後のブロックも一緒に表示(スクリーンショットの「前の文脈 / 該当箇所 / 後の文脈」3カラム)
   - 効果: 抜粋だけだと文脈が分からず誤読しやすい問題を解消
2. **同義語辞書によるクエリ拡張**
   - 今: クエリの文字通りの意味でしか検索できない
   - オリジナル: `expansion_rules.json` に言い換え(例:「ACC OFF」⇔「ACC off/on」)を登録し、検索前にクエリを拡張
   - 効果: 表現ゆれ(略語・言い回しの違い)に強くなる
3. **スコアリングのバージョン管理**
   - 今: スコアの閾値(A/B/C/D)やコサイン類似度の使い方がコードに直書き
   - オリジナル: `SearchPolicy` にバージョンを付け、スコア計算式や閾値をコードと切り離して調整・巻き戻しできる
   - 効果: 精度チューニングの試行錯誤を安全にできる
4. **メーカー絞り込みの安全な自動判定**
   - 今: メーカーはフォルダ名から機械的に取得しているだけ
   - オリジナル: フォルダ構成のどの階層が絞り込み軸か(メーカーか車種か)を明示的に宣言させ、宣言の無いデータには誤った絞り込みボタンを出さない
   - 効果: データ構成が変わったときに誤動作しない
5. **ChromaDBによる本格的なベクトル索引**
   - 今: numpy配列を総当たりで計算(件数が今の規模なら体感差はほぼ無い)
   - オリジナル: ChromaDB(HNSW索引)で高速な近似最近傍探索
   - 効果: データが数万件規模になったときの速度対策(今は優先度低)
6. **TF-IDFフォールバック**
   - 今: 意味検索(ベクトル)のみ
   - オリジナル: 意味検索が弱い/使えないときにキーワード一致検索へ自動切替
   - 効果: 固有名詞・型番など、意味検索が苦手な検索語の取りこぼしを減らす
7. **(任意) 検索結果をもとにした回答生成(RAG化)**
   - 今: 検索結果(元の文章)をそのまま一覧表示
   - オリジナル: 上位チャンクをGPT-4oに渡して日本語で1つの回答文に合成(オプション、要APIキー)
   - 効果: 「結局どうすればいいか」を1文でまとめてくれる。やるとオリジナルの「検索+生成」モードと同じ立ち位置になる

### 優先度を下げたもの

- **しおりが粗い/無いPDF・仕様書の章階層復元**
  - 仕様書は当分Markdown中心になる見込みのため保留。PDFがまた対象データになったときに再検討する

## 過去に判断・変更した点(経緯)

- 元の開発指示書はdocx前提だったが、実データがMarkdown/PDFだったため都度ingest.pyを書き換えた
- 一時的にMarkdown 810ファイル(メーカー多数)を投入した際、`tenants/dita`とトップ階層に重複文書があり、README記載の生成日時を比較して新しい方(`tenants/dita`)を優先する重複排除を実装した(現在のhonda/subaruデータでは重複なし)
- PDFデータ投入時、AI生成データの一部に同一文言が異常に長く繰り返される破損段落があったため、1段落あたりの強制分割を最大2回までに制限する安全策を入れた(`MAX_SLICES_PER_PARAGRAPH`)
- メーカー絞り込みは「全体上位N件を取ってから絞り込む」実装だと件数の少ないメーカーが0件になるバグがあったため、「対象メーカーのみで類似度計算をやり直す」実装に修正済み

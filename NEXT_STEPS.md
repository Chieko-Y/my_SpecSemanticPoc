# 進捗メモ(次回はここから)

## 今の状態

- データ: `data/honda`, `data/subaru` 配下のMarkdown(286文書 / 1265チャンク(2026-09-21にデータ入れ替え))
- Phase 1〜5(取込・ベクトル化・CLI検索・API・画面)は動作確認済み
- 現在のスコアリングは`v3`(2026-09-21に実測で閾値を見直し)
- 機能拡張の1〜4・6(下記)も動作確認済み
- 起動コマンド:
  ```
  cd "C:\Users\DIT-G-071\Desktop\work\AI\2026\Project\work\my_app\SpecSemanticPoc"
  python -m uvicorn app.main:app --host 127.0.0.1 --port 8000
  ```
  → http://127.0.0.1:8000/
- データを変更したときだけ `python app/ingest.py` → `python app/embed.py` を再実行する
- アプリの機能・構成の詳しい記録は `docs/DEVELOPMENT_RECORD.md` を参照(毎回更新)

## 次回の再開ポイント(2026-09-21終了時点)

1. **データ入れ替えは完了**: 旧データを削除し、MDは286件・1265チャンク。制御文字の破損は全ファイルで
   0件になり、`ingest.py` → `embed.py` も再実行済み(テキストとベクトルの件数一致を確認)。
   ※検索画面で最終確認するときは、サーバーを再起動してから行う
2. 取込時の制御文字(``〜``)除去は、元データが直ったため**未実装のまま**。
   今後また破損データが混ざる場合に備えて入れるかは、ユーザーの判断待ち(必須ではない)
3. **次の本命: 7. RAG化(回答生成)**。AIのAPIキーが必要なので、まず「以前使っていたGeminiのキーが
   使えるか」をユーザーに確認する。その後、概念の説明 → 実装 → 動作確認の順で1つずつ進める
4. 余裕があれば: フォルダ構成の変更(honda 3車種 / subaru 3車種、`ivi`配下の
   `generated`・`overlay`・`published`)に合わせて、`data/facets.json`(絞り込み軸)が
   新しい車種・年式を正しく拾えているか確認する

### データ品質の課題
- 解決済み(2026-09-21): 制御文字の破損が入っていた`23-map-screen.md`・`README.md`は、
  データの差し替えで直った

### Git(ブランチ)の状況
- `main`: 通常版(numpy方式)。普段はこちらを使う
- `try-chromadb`: ChromaDB方式のお試し版。同じフォルダ内でブランチを切り替えて使う
  (`git checkout main` / `git checkout try-chromadb`。切替前にサーバーを止める)。
  起動は`set SEARCH_BACKEND=chroma`のあと通常どおりuvicorn。実運用の規模になったら取り込む

## データ方針(2026-09-17追記)

仕様書は当分PDFではなく、今あるMarkdown(`data/honda`, `data/subaru`)が中心になる見込み。
そのため「PDFのしおりが粗い場合の章階層復元」は当面優先度低。現状のMarkdownは見出し(#, ##)が
きちんと付いているため、その問題自体があまり発生しない。

## 完了した機能拡張

ユーザーが「順番に理解しながら進めたい」とのことで、精度への効果順に1つずつ
「これは何をするものか」を説明→実装→動作確認、を1セットとして進めてきたもの。

1. **前後文脈の表示** ✅(2026-09-18)
   ヒットしたチャンクの前後にある文章も一緒に表示し、抜粋だけでは文脈が分からず
   誤読しやすい問題を解消。
2. **同義語辞書によるクエリ拡張** ✅(2026-09-18)
   `app/expansion_rules.json`に言い換え(例:「BT」⇔「Bluetooth」)を登録し、検索前に
   クエリを拡張。表現ゆれ(略語・言い回しの違い)に強くなる。
3. **スコアリングのバージョン管理** ✅(2026-09-18)
   `app/scoring.py`の`SearchPolicy`にバージョンを付け、スコア計算式や閾値をコードと
   切り離して調整・巻き戻しできるようにした。
4. **メーカー絞り込みの安全な自動判定** ✅(2026-09-18)
   `data/facets.json`でフォルダ構成のどの階層が絞り込み軸か(メーカーか車種か)を
   明示的に宣言させ、宣言の無いデータには誤った絞り込みボタンを出さないようにした。

## 今後の機能拡張候補(精度への効果順・全リスト)

5. **ChromaDBによる本格的なベクトル索引** 🧪 お試し済み・mainには未反映(2026-09-21)
   - 実装は`try-chromadb`ブランチにある。1250件ではnumpy 11.8ms / Chroma 13.2msで速度差なし、
     上位10件は8問中7問が完全一致(1問は近似探索で1件入れ替わり)
   - 方針: 今はmainに入れない。データが数万件規模になった実運用時に取り込む
     (`SEARCH_BACKEND=chroma`で切替、`python app/build_chroma.py`で索引作成)
   - 現状: numpy配列を総当たりで計算(件数が今の規模なら体感差はほぼ無い)
   - 目指す姿: ChromaDB(HNSW索引)で高速な近似最近傍探索
   - 効果: データが数万件規模になったときの速度対策(今は優先度低)
6. **TF-IDFフォールバック** ✅(2026-09-21)
   - `app/keyword_search.py`。意味検索が「該当なし」または型番らしい英数字を含むとき
     キーワード一致検索に自動切替(一致ゼロなら意味検索を維持)。画面に注記、結果に`mode`付与
   - 閾値は実測に基づきv3へ見直し済み(該当あり境界0.30→0.45)。詳細は`docs/DEVELOPMENT_RECORD.md`
7. **(任意) 検索結果をもとにした回答生成(RAG化)**
   - 現状: 検索結果(元の文章)をそのまま一覧表示
   - 目指す姿: 上位チャンクをAIに渡して日本語で1つの回答文に合成(オプション、要APIキー)
   - 効果: 「結局どうすればいいか」を1文でまとめてくれる。検索に加えて「生成」も行う
     モードを持たせることになる

### 優先度を下げたもの

- **しおりが粗い/無いPDF・仕様書の章階層復元**
  - 仕様書は当分Markdown中心になる見込みのため保留。PDFがまた対象データになったときに再検討する

## 過去に判断・変更した点(経緯)

- 当初はdocx形式のデータを想定していたが、実データがMarkdown/PDFだったため都度ingest.pyを書き換えた
- 一時的にMarkdown 810ファイル(メーカー多数)を投入した際、`tenants/dita`とトップ階層に重複文書があり、README記載の生成日時を比較して新しい方(`tenants/dita`)を優先する重複排除を実装した(現在のhonda/subaruデータでは重複なし)
- PDFデータ投入時、AI生成データの一部に同一文言が異常に長く繰り返される破損段落があったため、1段落あたりの強制分割を最大2回までに制限する安全策を入れた(`MAX_SLICES_PER_PARAGRAPH`)
- メーカー絞り込みは「全体上位N件を取ってから絞り込む」実装だと件数の少ないメーカーが0件になるバグがあったため、「対象メーカーのみで類似度計算をやり直す」実装に修正済み
